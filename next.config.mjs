/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: { ignoreDuringBuilds: true },
  // optional while wiring things
  // typescript: { ignoreBuildErrors: true },
};

export default nextConfig;
